/**
 * API pública del feature `dashboard`.
 *
 * Expone únicamente la vista principal que `app/` compone en `/dashboard`. `api/`,
 * `hooks/`, `types.ts`, `formato.ts`, `mensajes-error.ts` y `TarjetaResumen` son internos.
 */
export { ResumenDashboard } from "@/features/dashboard/components/ResumenDashboard";
