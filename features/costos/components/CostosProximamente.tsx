import { Proximamente } from "@/shared/ui";

/** Marcador de posición del módulo de costos, bloqueado por el backend. */
export function CostosProximamente() {
  return (
    <Proximamente
      titulo="Costos"
      descripcion="Aquí registrarás los costos operativos y sanitarios para calcular la rentabilidad de cada ciclo."
    />
  );
}
