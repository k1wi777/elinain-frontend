import { Proximamente } from "@/shared/ui";

/** Marcador de posición del módulo de ciclos, bloqueado por el backend. */
export function CiclosProximamente() {
  return (
    <Proximamente
      titulo="Ciclos"
      descripcion="Aquí gestionarás los ciclos de engorde: inicio, avance de pesos y cierre de cada lote."
    />
  );
}
