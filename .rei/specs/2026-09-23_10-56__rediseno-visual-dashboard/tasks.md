# Tareas

- [x] T1 — Adaptar `app/(dashboard)/layout.tsx` al shell oscuro premium, manteniendo sin cambios los enlaces de navegación y el cierre de sesión. (R1, R2, R7, R8)
- [x] T2 — Rediseñar `app/(dashboard)/dashboard/page.tsx` con encabezado, jerarquía de página y accesos estilizados a los dos reportes existentes, conservando sus rutas. (R1, R2, R4, R7, R8)
- [x] T3 — Actualizar `TarjetaResumen` y la composición de `ResumenDashboard` para agrupar y destacar las métricas ya disponibles según su importancia financiera u operativa, sin alterar la consulta ni los formateadores. (R1, R3, R7, R8)
- [x] T4 — Alinear el skeleton de `ResumenDashboard` y `app/(dashboard)/loading.tsx` con la composición final, y presentar el error existente en una superficie oscura accesible. (R5, R6, R7, R8)
- [x] T5 — Revisar responsive, foco visible, semántica y contraste en la pantalla; ejecutar V1 (`npm run format:check`), V2 (`npm run lint`), V3 (`npm run typecheck`) y documentar la evidencia V4 reportada por el usuario (`npm test -- --runInBand`: 44 suites y 366 tests), dejando instrucciones para V5 (validación visual manual). (R1–R8)
